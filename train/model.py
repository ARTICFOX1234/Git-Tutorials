# from train_data import data



class Question:
    
    def __init__(self, q_name, q_seats, q_fare):

        self.name = q_name
        self.seats = q_seats
        self.fare = q_fare

# new_q = Question(data[0]['name'],data[0]['seats'],data[0]['fare'])

# print(new_q.name)